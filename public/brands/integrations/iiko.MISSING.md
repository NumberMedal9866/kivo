# Logo asset notes

**Uzcard** (`uzcard.svg` / `uzcard.png`) — still missing:

- Wikimedia Commons has no file.
- uzcard.uz exposes only `corp_logo.*.svg`, which is the mark of their
  corporate group ("Корпорация Счастья"), **not** the Uzcard payment brand —
  do not use it.
- Until an official file is added, the site renders a neutral text tile.

**iiko** (`iiko.svg`) — present, but note its origin: it is a vector
recreation of the wordmark artwork supplied by the site owner (no official
standalone asset is exposed by iiko.ru / iiko.co.uk). Replace it with the
official brand file from the iiko partner portal when available.

**Action required:** request the official Uzcard and iiko files from the
respective brand teams, save them here, and update this file.
