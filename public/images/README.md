# Product imagery

Real product photos/renders belong here. Expected filenames, dimensions and
art direction for every image are defined in `src/content/media.ts`, and the
full replacement workflow is documented in `docs/MEDIA_REPLACEMENT_GUIDE.md`.

Until a file exists, the site renders a designed SVG/CSS placeholder for it.
